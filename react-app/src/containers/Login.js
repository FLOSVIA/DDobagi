import { Authentication } from "components";

class Login extends React.Component {
  render() {
    return (
      <div>
        <Authentication />
      </div>
    );
  }
}

<Authentication mode={true} />;
